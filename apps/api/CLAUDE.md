# API Package Map

`src/schemas/` defines contracts, `src/audits/` contains pure rules, `src/importers/` parses bounded packages, `src/plans/` validates proposals, and `src/auth/` verifies caller identity. `app.py` only assembles routes.

