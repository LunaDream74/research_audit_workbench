# Submission screenshots

These screenshots were captured from the deployed public demo on September 2, 2026.

1. `01-live-webmcp-scope.png`: selected runs, decision question, and live page scope available to the browser agent. The capture used standard Chrome, so the feature-detected manual fallback badge is visible.
2. `02-comparability-finding.png`: deterministic audit result that retains both scores while rejecting a direct ranking under mismatched conditions.
3. `03-side-by-side-evidence.png`: recorded candidate manifests, JSON pointers, source hashes, and a clearly limited illustrative example.
4. `04-challenge-revision.png`: researcher context changes the interpretation without removing the 200-versus-1,000 limitation.
5. `05-immutable-approval-history.png`: exact plan version, SHA-256 digest, researcher approval, and the explicit absence of execution permission.
